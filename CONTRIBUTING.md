# 一起共建Alun旅行规划

欢迎带来旅行规划方法、交付体验、宿主适配和文档方面的改进。

## 提交反馈

在仓库 Issue 中说明你的使用场景、期望行为和实际表现。用于复现的行程请使用虚构地点与示例数据；令牌、身份信息和真实预订凭证保留在自己的安全环境中。

## 提交改动

1. Fork 仓库并创建分支。
2. 围绕一个清楚的使用场景完成改动，保持 Markdown、HTML 和行程数据的一致性。
3. 如果影响脚本行为，运行相关测试；如果影响输出，实际查看生成文件。
4. 提交 Pull Request，说明用户会感受到的变化以及验证方式。

在仓库根目录运行全部本地测试：

```bash
python -m unittest discover -s scripts/tests -v
```

测试使用合成数据和模拟接口。执行测试无需配置 TikHub 令牌。

## 目录导航

| 目录 | 内容 |
| --- | --- |
| `SKILL.md` | Skill 入口和执行流程 |
| `references/` | 调研、规划、核查、交付与渠道说明 |
| `scripts/` | 数据校验、同源渲染、版本管理与 TikHub 适配 |
| `scripts/tests/` | 可重复运行的自动化测试 |
| `assets/` | 数据契约、HTML 模板、署名配置和示例数据 |
| `docs/` | 使用说明、可浏览示例与品牌图片 |

贡献内容采用本仓库的 [MIT License](LICENSE)。感谢你让每一次出发更从容。
